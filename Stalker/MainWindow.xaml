<Window x:Class="Stalker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Stalker.Views"
        Title="Stalker" Height="700" Width="1000"
        MinHeight="600" MinWidth="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">

    <Border Background="{StaticResource BackgroundBrush}"
            CornerRadius="12"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Border Grid.Row="0"
                    Background="{StaticResource SurfaceBrush}"
                    CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="16,0">
                    <TextBlock Text="Stalker" FontSize="13" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                                VerticalAlignment="Center">
                        <Button Content="&#x2212;" Width="28" Height="28"
                                Style="{StaticResource GhostButton}"
                                Padding="0" FontSize="16"
                                Click="MinimizeButton_Click"/>
                        <Button Content="&#x2715;" Width="28" Height="28"
                                Style="{StaticResource GhostButton}"
                                Padding="0" FontSize="14"
                                Foreground="#EF4444"
                                Click="CloseButton_Click"
                                Margin="4,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content + Sidebar -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="64"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0"
                        Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="0,0,1,0">
                    <StackPanel VerticalAlignment="Center">
                        <Button Style="{StaticResource GhostButton}"
                                Width="48" Height="48" Padding="0"
                                ToolTip="Dashboard"
                                Click="NavDashboard_Click"
                                x:Name="NavDashboard">
                            <TextBlock Text="&#xE7FC;" FontFamily="Segoe MDL2 Assets" FontSize="18"/>
                        </Button>
                        <Button Style="{StaticResource GhostButton}"
                                Width="48" Height="48" Padding="0"
                                ToolTip="Configurações"
                                Click="NavSettings_Click"
                                x:Name="NavSettings"
                                Margin="0,8,0,0">
                            <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="18"/>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- Content area -->
                <ContentControl Grid.Column="1" x:Name="MainContent"/>
            </Grid>
        </Grid>
    </Border>
</Window>
