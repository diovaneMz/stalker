<UserControl x:Class="Stalker.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Stalker.ViewModels"
             Background="{StaticResource BackgroundBrush}">

    <UserControl.DataContext>
        <vm:LoginViewModel/>
    </UserControl.DataContext>

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="360">

            <!-- Logo / Título -->
            <TextBlock Text="👁 STALKER"
                       FontSize="32" FontWeight="Bold"
                       Foreground="{StaticResource AccentBrush}"
                       HorizontalAlignment="Center"
                       FontFamily="Segoe UI"
                       Margin="0,0,0,8"/>

            <TextBlock Text="Rastreador de tempo para jogos"
                       FontSize="14"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,48"/>

            <!-- Discord -->
            <Button Style="{StaticResource AccentButton}"
                    Command="{Binding LoginWithDiscordCommand}"
                    Height="48"
                    Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE72E;" FontFamily="Segoe MDL2 Assets" FontSize="16"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Entrar com Discord" VerticalAlignment="Center" FontSize="14"/>
                </StackPanel>
            </Button>

            <!-- Google -->
            <Button Height="48"
                    Command="{Binding LoginWithGoogleCommand}"
                    Cursor="Hand"
                    BorderThickness="1"
                    BorderBrush="{StaticResource BorderBrush}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{StaticResource SurfaceBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="16,8">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="G" FontWeight="Bold" FontSize="16"
                                           Foreground="#EA4335"
                                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Entrar com Google" FontSize="14"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!-- Status -->
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="12"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,0"/>

        </StackPanel>
    </Grid>
</UserControl>
